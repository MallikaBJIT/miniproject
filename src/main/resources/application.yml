spring:
  datasource:
    url: jdbc:mysql://localhost:3306/miniprojectdb
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: mallikaju307@gmail.com
    password: htdsvrypqigponpd
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
